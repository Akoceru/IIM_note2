{% extends '::base.html.twig' %}
{% block title %}
    {{ parent() }} - Add grade
{% endblock %}
{% block content %}
    <title>Add a grade</title>

    {{ form_start(form) }}
    {{ form_rest(form) }}

    <input type="submit" value="Save" class="btn btn-default">
    {{ form_end(form) }}
{% endblock %}